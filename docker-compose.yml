services:
  rest_api:
    build: ./rest_api
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./rest_api/swagger.yml:/app/swagger.yml  # Ajout du fichier Swagger

  grpc_service:
    build: ./grpc_service
    ports:
      - "50051:50051"
    volumes:
      - ./data:/app/data

  nginx:
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - rest_api
      - grpc_service
